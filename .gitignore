# Secrets — the KIE API key must never be committed.
.env
.env.local
.env.*.local
*.pem
credentials.json

# Generated media from KIE jobs
kie-out/

node_modules/
npm-debug.log*
.DS_Store

# Regenerable @2x render (run design/8.8/render.mjs)
design/**/*@2x.png

# Brand fonts are licensed separately — install from Drive, never commit
design/**/fonts/
