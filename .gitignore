# Secrets — never commit API keys
.env
.env.*
!.env.example
*.key
.kie.key
.claude/settings.local.json

# Local artifacts
node_modules/
kie-out/

# Font is a Fontspring DEMO — licensed copy must not be committed either
banners/assets/fonts/
